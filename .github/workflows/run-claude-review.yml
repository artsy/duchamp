name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]

jobs:
  claude-review:
    # TODO: Change back to @main before merging!
    uses: artsy/duchamp/.github/workflows/claude-review.yml@pr-review-specialist
    secrets:
      anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}
