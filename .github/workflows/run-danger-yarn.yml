name: ☢️ Danger - Yarn

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  run-danger-yarn:
    uses: ./.github/workflows/danger-yarn.yml
    secrets:
      danger-token: ${{ secrets.DANGER_TOKEN }}
